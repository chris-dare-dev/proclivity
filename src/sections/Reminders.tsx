export function Reminders() {
  return (
    <div className="section-empty">
      Reminders go here. Backed by chrome.alarms + chrome.notifications — set a
      time, optionally link to a todo, optionally make it recurring.
    </div>
  );
}
