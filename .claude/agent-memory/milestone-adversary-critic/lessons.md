# milestone-adversary-critic lessons

This file is an **append-only log**. Each entry records what was learned during a single
Phase 3 adversarial critique. Never delete or rewrite previous entries. To correct a
prior entry, append a new one that references the old timestamp.

Entry format (defined in `.claude/agents/milestone-adversary-critic.md` § Memory protocol):

```
## <ISO-8601 UTC timestamp> · milestone:<id> · status:<status>
- **Bottleneck observed:** ...
- **What worked:** ...
- **What didn't:** ...
- **Reusable lesson:** ...
```

---

<!-- Entries will be appended below this line by the milestone-adversary-critic agent. -->

## 2026-05-20T18:47:00Z · milestone:frontend-uplift-2026q2-m3 · status:complete
- **Bottleneck observed:** Hex/rgba-to-CSS-token swaps that assume one theme silently regress in the other. `rgba(0,0,0,X)` → `color-mix(in srgb, var(--bg) X%, transparent)` works in dark theme (where `--bg` is near-black) but inverts in light (where `--bg` is near-white). Foreground-derived (`--text`-mix) overlays are theme-invariant; background-derived overlays are not.
- **What worked:** Independent `npm run build` re-run inside the critic verified the implementer's 234.02 kB claim — caught nothing wrong but gave high-confidence anchor for the verdict. Cross-checking the implementer's "fully uncovered files" list against actual `grep prefers-reduced-motion src/**/*.css` output caught two false positives (sections.css + ClosedTodosView.css both ALREADY had per-site guards), which propagated into a doc-drift finding in theme.css.
- **What didn't:** Initially trusted the research synthesis's enumeration of "fully-uncovered" vs "asymmetric" CSS files without re-grepping. The synthesis classification was wrong on 2 of 9 files — and the implementer carried that error forward verbatim.
- **Reusable lesson:** When critic-ing a milestone whose research synthesis enumerates files into pre-baked buckets ("uncovered," "asymmetric," "fully-guarded"), independently re-derive the bucketing via grep before judging fit. Synthesis classification is a hypothesis to verify, not a fact. Also: any hex→token swap that touches a value whose semantic role depends on a fixed luminance direction (shadows, dim-tints, darken-overlays) needs explicit theme-symmetry inspection — prefer `--text`-derived overlays over `--bg`-derived when the goal is "always darken" or "always lighten relative to the content."

## 2026-05-20T22:00:00Z · milestone:frontend-uplift-2026q2-m5 · status:complete
- **Bottleneck observed:** CSS selectors keyed on a class name (`[data-staggered] .todo-list li`) silently fan out to every `<ul>` that carries that class within the scoped subtree — including `card-fallback-list` and `ArchivedSprintRow` `<ul>`s. The synthesis asserted "card mode exclusion is implicit" but `TodoCardSection.tsx:175` renders `<ul className="todo-list card-fallback-list">`, so the exclusion was illusory. The selector matched but the call site didn't pass `index={idx}`, producing a silent visual degradation (all rows animate with delay=0) rather than a build failure.
- **What worked:** [CONFIRMED] Independent `npm run build` to verify the implementer's chunk-size claim — saved 30s and gave high-confidence anchor for the verdict. Grep'ing `todo-list` across `src/**` to enumerate every `<ul>` that carries the class — that's how the card-mode + archived-sprint matches surfaced. Reading both the synthesis AND the implement/synthesis to compare claimed vs actual coverage.
- **What didn't:** Initially trusted the synthesis's §3.6 card-mode-exclusion claim. Had to re-grep `todo-list` across the codebase to discover it was wrong. Same lesson as m3 confirmed: synthesis classifications are hypotheses to verify.
- **Reusable lesson:** For any milestone that introduces a `[data-X="true"] .someClass <elem>` style selector, enumerate every `<ul>`/`<li>` (or relevant tag) carrying `.someClass` via repo-wide grep BEFORE judging whether scope/exclusion claims hold. The class is the selector's surface area; React composability means the same class can appear in unexpected subtrees. Also: commit-subject length ≤50 after the prefix is a hard CLAUDE.md rule — measure with `printf '%s' "..." | wc -c`, not by eyeball; off-by-2 is the most common drift.

## 2026-05-20T22:30:00Z · milestone:frontend-uplift-2026q2-m4 · status:complete
- **Bottleneck observed:** A child-combinator selector keyed on tag name + attribute negations (`.content > div:not([hidden]):not([data-leaving]):not([data-staggered])`) fans out to NON-tabpanel `<div>` children of the same parent — here, `<div className="section-empty">` at App.tsx:596 was a `:not()`-passing match that would receive the 220 ms fade-in on every visibilityToggle re-render. The implementer's mental model said "child tabpanel divs," but the selector said "any direct div child." Same class as the m5 lesson, different shape: not a className fan-out, a tag+attribute fan-out within the same DOM parent.
- **What worked:** [CONFIRMED] Independent `npm run build` (235.47 kB matches implementer claim exactly) — third consecutive run validates this as the canonical anchor for chunk-budget verdicts. Reading App.tsx end-to-end to enumerate every direct `<div>` child of `.content` (not just the tabpanels) is what surfaced `.section-empty`. The synthesis §3.5 listed the selector and the implementer copied it verbatim; neither caught the empty-state fan-out. Confirms m5 lesson: synthesis selector prescriptions are hypotheses to verify by reading the full parent's children, not the just listed tabpanels.
- **What didn't:** n/a — the m3 + m5 lesson "verify synthesis classifications by repo-wide grep/read" worked again. Took ~3 reads to land the empty-state catch.
- **Reusable lesson:** For ANY new CSS selector with `:not()` negations against a child-combinator (`.parent > tag:not(...)`), enumerate ALL direct children of `.parent` by reading the full JSX, not just the children the synthesis lists. Negation selectors are inclusive-by-default; their reach equals "all matching tag + all not-explicitly-excluded attributes." Also confirms: tabpanel selectors should key on `[role="tabpanel"]` (the existing ARIA marker) rather than tag+attribute exclusions — semantic anchor beats negation chain. Also: 3 of 3 recent milestones used independent `npm run build` as the verdict anchor — promote this to the "default first action" in the critic checklist.

## 2026-05-20T23:30:00Z · milestone:frontend-uplift-2026q2-m6 · status:complete
- **Bottleneck observed:** New `.todo-item:hover` rule did NOT carve out `.card-fallback-list` ancestor scope. Synthesis §3.1–§3.7 covered theme-invariance, dual-guard reduced-motion, stagger precedence, and stacking-context safety, but did NOT enumerate card-mode interaction — exactly the m5 trap shape (same class reused in card-fallback context). Also fan-out into `ArchivedSprintRow` (read-only) rows where `onEdit` is intentionally absent — lift fires uniformly, signalling editability the row no longer affords. Both are MEDIUM, not HIGH (visual inconsistency, not correctness).
- **What worked:** [CONFIRMED] Independent `npm run build` (235.57 kB matches implementer claim to the byte, identical to m4 baseline — pure CSS edit delta of 0). Fourth consecutive milestone confirming this as canonical. [CONFIRMED] Repo-wide `grep -rn "todo-item\|todo-list"` enumerated every render site BEFORE judging selector scope — this surfaced TodoCardSection.tsx:175 + RemindersCardSection.tsx:407 (card-fallback) and SprintManager.tsx:609 (ArchivedSprintRow). [CONFIRMED] `printf '%s' "..." | wc -c` measured commit subject (42 chars) vs the 50 cap. [CONFIRMED] m5 lesson: synthesis scope claims are hypotheses to verify by enumerating all class renders.
- **What didn't:** n/a — the established workflow (independent build + grep + read full JSX) caught both M-findings on first pass. No false positives, no missed traps.
- **Reusable lesson:** When the same `.className` is used across BOTH list-mode AND fallback-mode (card-fallback, archived, read-only contexts), NEW selectors keyed on that class should default to explicit scope (e.g. `:not(.card-fallback-list)`) and synthesize an architectural decision per affordance-firing-context. The m5-s9 stagger pattern (`:not(.card-fallback-list)`) is the canonical template; m6's lift should have applied the same scoping prophylactically. Promote to a Phase 1 (researcher/synthesis) check: "For each new selector S keyed on class C, enumerate every render site of C and assert S's reach is intentional at each site." Also: when implementer's prose anchor (e.g. "sections.css §stagger-reveal") refers to a section by name, verify the named anchor token actually exists in the file (this milestone's L2 doc-drift was anchor-name without the named token).

## 2026-05-21T01:30:00Z · milestone:frontend-uplift-2026q2-m7 · status:complete
- **Bottleneck observed:** AnimatePresence-driven exit windows (~180 ms) extend the period during which the focus-trap and event handlers stay live AFTER `open=false`. The rAF focus-restoration runs ~16 ms in; the remaining ~164 ms is a Tab-during-exit a11y window where `useFocusTrap` re-pulls focus into the closing modal. Synthesis OQ4 explicitly considered `inert` and chose to skip — the riskier call. This is the m4-s11 "focus discipline during animated exits" shape, repeated. Also: when the new milestone touches a component (Modal.tsx) consumed eagerly by multiple top-level surfaces (Gantt, ChartView, SprintManager, RemindersManager, ClosedTodosView), motion imports added at that scope tree-shake INTO the initial chunk, not the lazy modal chunks — synthesis prediction of +0 kB became +15.9 kB. The fix is an "animated Modal vs static Modal" file split.
- **What worked:** [CONFIRMED 5/5] Independent `npm run build` reproduced 251.47 kB to the byte. Fifth consecutive milestone — this is now the canonical verdict anchor. [CONFIRMED] `printf '%s' '...' | wc -c` measured commit subject at 43 chars under 50. [CONFIRMED] Repo-wide grep for orphan references (`modal-fade-in|modal-slide-in`) caught nothing because the deletion was clean — but the grep is what proved cleanliness. Reading SettingsModal.tsx end-to-end (line 547-592) confirmed `<>... </>` sibling structure for nested ConfirmDialog, dismissing the AnimatePresence-nesting concern. Confirmed `useStore()` destructure is read-only (only `state`, no `update`) for the storage-boundary axis.
- **What didn't:** n/a — the established workflow (build + grep + read end-to-end + measure subject) caught the Tab-during-exit a11y window on first pass.
- **Reusable lesson:** For AnimatePresence (or any animated-exit) milestones, ALWAYS flag the Tab-during-exit-window as HIGH if the focus-trap + event handlers stay wired during the exit and no `inert` attribute (or equivalent focus-tree removal) is applied. rAF focus restoration alone is INSUFFICIENT — it restores once, but the trap re-pulls on any subsequent Tab. The fix template is: `<m.div {...(!open ? { inert: '' } : {})} onKeyDown={(e) => { if (!open) return; handler(e); }}>`. Also: when motion imports are added to a Modal/Drawer/Sheet base that is eagerly consumed by N top-level surfaces, EXPECT the imports to land in the initial chunk, NOT the lazy children — synthesis predictions to the contrary are usually wrong. Promote to Phase 1 check: "Enumerate the eager consumers of any base component before predicting where new imports land in the chunk graph." Lastly: the `motion` scope drift (m4 M1, m5 M1, m7 M1 — three in a row) is past deferral; the CLAUDE.md scopes list edit should be bundled with any future motion milestone's rectifier pass.

## 2026-05-20T23:55:00Z · milestone:frontend-uplift-2026q2-m10 · status:complete
- **Bottleneck observed:** A new local helper that re-implements an upstream-library function "verbatim" but drops one token in a regex (here: `/iphone|ipad/i` vs the library's actual `/iphone|ipad|ipod/i`). The implement-synthesis comment "we replicate the same navigator.userAgent check here" is an assertion to verify, not a fact — diffed the library's compiled `dist/index.js` regex against the local helper's regex to catch the drift. Also: mixed-unit chunk-budget reporting (gzip vs raw) hides delta headroom — m10's implement-synthesis cited 83.52 kB gzip against a 260 kB raw target, making the actual 1.72 kB raw headroom invisible.
- **What worked:** [CONFIRMED 6/6] Independent `npm run build` reproduced 258.28 kB raw / 83.52 kB gzip to the byte — sixth consecutive milestone confirming this as canonical first action. [CONFIRMED] `printf '%s' '...' | wc -c` measured subject at 45 chars under 50. [CONFIRMED] Repo-wide `grep -rn 'addEventListener.*keydown' src/` proved the Modal.tsx escape handler boundary held. [CONFIRMED] m6 lesson: when a synthesis cites an external lib API by name (`isMacOS()` exported), the implementer's claim to "replicate it" must be diffed against the actual lib source. Comparing the local helper's regex tokens against `node_modules/<lib>/dist/index.js` literal text caught the `ipod` drop. [CONFIRMED] m7 lesson: scope drift (`deps` not in CLAUDE.md scopes list) is past deferral — bundle the CLAUDE.md amend with the milestone's rectifier pass.
- **What didn't:** n/a — the established workflow (build + grep + library-source diff + subject-length measure) caught all findings on first pass.
- **Reusable lesson:** For ANY milestone where the implementer flags a "deviation from synthesis" because an upstream lib API was misidentified, the critic MUST diff the implementer's local re-implementation against the lib's compiled source (e.g. `node_modules/<pkg>/dist/index.js`) — synthesis-claimed parity is a hypothesis. Also: when reporting chunk-budget verdicts, ALWAYS pair raw bytes with gzip bytes; the synthesis target unit (raw vs gzip) is not always the unit the build output prominently displays, and the unit mismatch hides headroom. Also: React `key={X}` props where X is a free-text string (token, registry-keys-field) silently collapse on duplicates — flag as LOW for current scope but recommend index-or-composite keys for any free-text key prop, as a defensive pattern.
