# {{STORY_ID}} — {{STORY_TITLE}}

**Parent epic:** {{EPIC_ID}}
**Parent milestone:** `{{MILESTONE_ID}}`  <!-- e.g. gantt-drag-m1 — the <slug>-mN ID for /milestone-pipeline -->
**Complexity:** {{COMPLEXITY}}  <!-- S (≤1d) | M (1–3d) | L (>3d, consider splitting) -->

## Description

{{STORY_DESC}}

## Acceptance criteria

{{ACCEPTANCE_CRITERIA}}

<!--
Format depends on shape:
  - Behavior-shaped story (state + trigger + observable outcome):
      Given <context>, When <event>, Then <outcome>.
  - Artifact-shaped story (properties the artifact must have):
      - [ ] specific testable condition
      - [ ] another specific testable condition

If you can't write the AC in 3+ bullets, the story is too small — merge.
If the AC needs more than ~7 bullets, the story is too big — split (SPIDR).
-->

## Verification

- [ ] Project check command passes (`npm run build`)
- [ ] {{VERIFICATION_STEP_1}}

## Dependencies

{{DEPENDENCIES}}

## Notes

{{NOTES}}

## Roadmap reference

Generated by `/roadmap` from `plans/{{SLUG}}-roadmap.md`. The
parent epic was placed in the {{LANE}} lane based on its MoSCoW + RICE
score; see the roadmap doc for the rationale.

To execute the parent milestone end-to-end, run:

```
/milestone-pipeline {{MILESTONE_ID}}
```

Where `{{MILESTONE_ID}}` is the `<slug>-mN` milestone ID for this story's
Now-lane epic (e.g. `gantt-drag-m1`, `reminders-recurrence-m1`). Milestone IDs
follow the `^[a-z0-9-]+-m[0-9]+$` format that `/milestone-pipeline` accepts.
The parent epic (`{{EPIC_ID}}`) is the roadmap concept-level grouping; the
milestone ID is what the pipeline greps.

---
*Generated by `/roadmap` — review and edit before creating.*
